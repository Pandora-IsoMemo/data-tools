# Rename Existing Names

Rename Existing Names

## Usage

``` r
renameExistingNames(newList, oldList)
```

## Arguments

- newList:

  (list) list with named object(s) to be appended to an existing list

- oldList:

  (list) list to be extended
