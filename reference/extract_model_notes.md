# Extract model notes from bundle import

Extracts README.txt contents from a bundle import.

## Usage

``` r
extract_model_notes(bundle_import)
```

## Arguments

- bundle_import:

  Result of import_bundle_zip().

## Value

Model notes (character) or NULL if not found.
