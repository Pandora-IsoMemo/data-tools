# Delete Columns UI

UI of the module

Server function of the module

## Usage

``` r
deleteColumnsUI(id)

deleteColumnsServer(id, preparedData)
```

## Arguments

- id:

  id of module

- preparedData:

  (reactive) selected data, possibly already modified
