# S3 method: Convert DataProcessLink to a plain list Converts a DataProcessLink object to a plain list, excluding class attributes.

S3 method: Convert DataProcessLink to a plain list Converts a
DataProcessLink object to a plain list, excluding class attributes.

## Usage

``` r
# S3 method for class 'DataProcessLink'
as.list(x, ...)
```

## Arguments

- x:

  The DataProcessLink object to convert.

- ...:

  Additional arguments (not used).

## Value

A plain list representation of the DataProcessLink x.
