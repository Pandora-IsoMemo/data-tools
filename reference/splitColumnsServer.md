# Split Columns UI

UI of the module

Server function of the module

## Usage

``` r
splitColumnsUI(id)

splitColumnsServer(id, preparedData)
```

## Arguments

- id:

  id of module

- preparedData:

  (reactive) (reactive) selected data, possibly already modified
