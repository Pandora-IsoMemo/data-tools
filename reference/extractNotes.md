# Extract Notes

Extracts the notes from the README.txt file.

## Usage

``` r
extractNotes(pathToUnzipped)
```

## Arguments

- pathToUnzipped:

  (character) path to the folder were the model (or inputs) was
  unzipped.

## Value

(character) notes
