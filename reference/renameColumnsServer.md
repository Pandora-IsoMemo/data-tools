# Rename Columns UI

UI of the module

Server function of the module

## Usage

``` r
renameColumnsUI(id)

renameColumnsServer(id, preparedData)
```

## Arguments

- id:

  id of module

- preparedData:

  (reactive) selected data, possibly already modified
