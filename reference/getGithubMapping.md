# Get Github Mapping

Maps the R package name to the respective Github repository

## Usage

``` r
getGithubMapping(rPackage = "")
```

## Arguments

- rPackage:

  (character) name of the R package (as in the Description file), must
  be empty or specified in the config file of the package DataTools
