# globals

Contains the package's global variables

## Usage

``` r
pkg.env
```

## Format

An object of class `environment` of length 3.

## Fields

- `api_key`:

  The OpenAI API key

- `url.completions`:

  The URL for the completions endpoint
