# Prefix Systime

Prefixes a file name with the current time

## Usage

``` r
prefixSysTime(fileString)
```

## Arguments

- fileString:

  (character) file name

## Value

(character) file name with prefix of current time
