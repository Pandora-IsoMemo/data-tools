# Format Column Names

Replaces all not alpha-numeric characters in the names of columns with a
dot.

## Usage

``` r
formatColumnNames(df, silent = FALSE)
```

## Arguments

- df:

  (character) data

- silent:

  (logical) set TRUE prevent notification of warnings
