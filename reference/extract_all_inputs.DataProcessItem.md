# S3 method: Extract unique inputs for DataProcessItem Extracts unique user inputs from a DataProcessItem object.

S3 method: Extract unique inputs for DataProcessItem Extracts unique
user inputs from a DataProcessItem object.

## Usage

``` r
# S3 method for class 'DataProcessItem'
extract_all_inputs(object, ...)
```

## Arguments

- object:

  The DataProcessItem object to extract from.

- ...:

  Additional arguments (not used).

## Value

A named list of unique user inputs.
