# Get CKAN Group Choices

Get choices that will be available in the ckanGroup (network) input

## Usage

``` r
getCKANGroupChoices(groupList = data.frame())
```

## Arguments

- groupList:

  (data.frame) optional, output of callAPI() from a previous call to the
  Pandora API.
